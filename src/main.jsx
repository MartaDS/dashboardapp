import React from 'react';
import ReactDOM from 'react-dom/client';
import CancerCharityDashboard from './App.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <CancerCharityDashboard />
  </React.StrictMode>
);
